{
  "name": "kayak-cypress",
  "version": "1.0.0",
  "description": "Kayak.es Test Automation with Cypress",
  "scripts": {
    "cy:open": "cypress open",
    "test": "cypress run",
    "test:cloud": "cypress run --record",
    "test:cloudparallel": "cypress run --record --parallel"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fecresgam/cypress-example.git"
  },
  "dependencies": {
    "-": "^0.0.1",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^13.4.0",
    "eslint-plugin-cypress": "^2.15.1",
    "save-dev": "^0.0.1-security"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
